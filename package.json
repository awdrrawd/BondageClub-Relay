{
  "name": "bondageclub-relay",
  "private": true,
  "type": "module",
  "scripts": {
    "build:relay": "node scripts/build-relay.mjs",
    "source:fetch": "node scripts/source.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "preview:entry": "node scripts/preview-entry.mjs",
    "check:assets": "node scripts/check-assets.mjs",
    "check": "npm test && npm run build && npm run check:assets"
  },
  "engines": { "node": ">=22.13" }
}
