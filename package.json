{
  "name": "bondageclub-relay",
  "private": true,
  "type": "module",
  "scripts": {
    "build:relay": "node scripts/build-relay.mjs",
    "build": "node scripts/build-relay.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm test && npm run build"
  },
  "engines": { "node": ">=22.13" }
}
